<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="cardViewStyle">@style/CardView</item>
    </style>

    <style name="DescTextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAlignment">center</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="carview_text" parent="Base.TextAppearance.AppCompat">
        <item name="android:paddingEnd">@dimen/carview_padding</item>
        <item name="android:paddingStart">@dimen/carview_padding</item>
    </style>

    <style name="detailview_text" parent="Base.TextAppearance.AppCompat">
        <item name="android:paddingEnd">@dimen/detail_view_padding</item>
        <item name="android:paddingStart">@dimen/detail_view_padding</item>
        <item name="android:textSize">17sp</item>
        <item name="android:paddingBottom">@dimen/carview_padding</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>

</resources>
